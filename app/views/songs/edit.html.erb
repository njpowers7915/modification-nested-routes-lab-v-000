<h1>Editing <%= @song.title %></h1>
<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.hidden_field_tag 'song[artist_id]', @song.artist_id %>
  <br />

  <%= f.submit %>
<% end %>
